# Infrragment
A PDF splitter that recovers individual documents from multi-hundred-page loan packages. Classifies each page as First/Other/Last using heuristics + TF-IDF, then stitches page ranges into separate PDFs with a manifest.json. No training data required.
